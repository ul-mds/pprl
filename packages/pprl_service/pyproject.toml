[tool.poetry]
name = "pprl-service"
version = "0.1.0"
description = ""
authors = ["Maximilian Jugl <Maximilian.Jugl@medizin.uni-leipzig.de>"]
readme = "README.md"
package-mode = false
license = "MIT"

[tool.poetry.scripts]
pprl_service = "pprl_service.main:run_server"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.111.0"
pprl-core = {path = "../pprl_core"}
pprl-model = {path = "../pprl_model"}

[tool.poetry.group.dev.dependencies]
pytest = "^8.2.2"
ruff = "^0.4.10"
faker = "^26.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
